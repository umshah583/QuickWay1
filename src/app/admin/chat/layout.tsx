export const metadata = {
  title: 'Admin Chat | Quickway',
  description: 'Test real-time chat functionality',
};

export default function ChatLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
